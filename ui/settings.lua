return {
    VERSION = "1.0";
    UI_KEYBIND = Enum.KeyCode.RightControl,
    UI_AUTOSHOW = true;

    BALL = {
        CATCHING = {
            ENABLED = true,
            DISTANCE = 5;
        };

        SWATTING = {
            ENABLED = true;
            DISTANCE = 5;
        };
    };

    PHYSICS = {
        LADDER = {
            ENABLED = true;
        }
    }
}